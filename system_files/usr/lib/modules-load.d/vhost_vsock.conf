# Claude Desktop's Cowork tab needs /dev/vhost-vsock. udev's static_node rule
# autoloads the module on first open; loading it at boot makes the app's
# one-shot launch-time capability check deterministic.
vhost_vsock
